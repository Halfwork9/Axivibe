import { Card, CardHeader, CardTitle } from "@/components/ui/card";

function StripeReturnPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Redirecting to Stripe Checkout... Please wait!</CardTitle>
      </CardHeader>
    </Card>
  );
}

export default StripeReturnPage;
